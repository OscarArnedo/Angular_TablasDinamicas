import { FiltroAutorPipe } from './filtro-autor.pipe';

describe('FiltroAutorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAutorPipe();
    expect(pipe).toBeTruthy();
  });
});
